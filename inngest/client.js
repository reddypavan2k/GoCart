import { Inngest } from "inngest";

export const inngest = new Inngest({
  id: "gocart-ecommerce",          // unique identifier for your app
  name: "GoCart E-Commerce App",   // human-readable name (shows in Inngest UI)
});
